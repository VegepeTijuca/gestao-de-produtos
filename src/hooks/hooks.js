import { useEffect, useState } from "react"
import { createRoot } from "react-dom/client"

function useTeste() {
   const [teste, setTeste] = useState("abuble");

   return (
    <>
        
    </>
   )
    /*  Ele deve expor: listar, adicionar, editar, excluir e filtrar produtos. Toda alteração deve chamar saveProducts para persistir. */
};